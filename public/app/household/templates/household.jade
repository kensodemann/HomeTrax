.container
  .view-section
    .text-center
      strong {{ctrl.household.name}}
    .text-center {{ctrl.household.addressLine1}}
    .text-center {{ctrl.household.addressLine2}}
    .text-center {{ctrl.household.city}}, {{ctrl.household.state}} {{ctr.household.postal}}
    .text-center {{ctrl.household.phone}}
  
  div(bs-active-pane="ctrl.tabs.activeTab", bs-tabs)
    div(ng-repeat="tab in ctrl.tabs", title="{{ tab.title }}", ng-bind="tab.content", bs-pane)